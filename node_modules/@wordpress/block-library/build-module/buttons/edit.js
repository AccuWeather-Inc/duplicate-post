import { createElement } from "@wordpress/element";

/**
 * WordPress dependencies
 */
import { __experimentalAlignmentHookSettingsProvider as AlignmentHookSettingsProvider, __experimentalUseInnerBlocksProps as useInnerBlocksProps, useBlockProps } from '@wordpress/block-editor';
/**
 * Internal dependencies
 */

import { name as buttonBlockName } from '../button/';
var ALLOWED_BLOCKS = [buttonBlockName];
var BUTTONS_TEMPLATE = [['core/button']]; // Inside buttons block alignment options are not supported.

var alignmentHooksSetting = {
  isEmbedButton: true
};

function ButtonsEdit() {
  var blockProps = useBlockProps();
  var innerBlocksProps = useInnerBlocksProps(blockProps, {
    allowedBlocks: ALLOWED_BLOCKS,
    template: BUTTONS_TEMPLATE,
    orientation: 'horizontal'
  });
  return createElement(AlignmentHookSettingsProvider, {
    value: alignmentHooksSetting
  }, createElement("div", innerBlocksProps));
}

export default ButtonsEdit;
//# sourceMappingURL=edit.js.map